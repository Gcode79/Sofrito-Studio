# Ops — Decision Log (standing + open)

> Log discipline: every executed decision records what, when, why (evidence), who. Open decisions list options + recommendation + owner status. Sign-off rule from business-model.md is always in force: no publish/deploy/spend/contact/pricing without owner approval.

## Executed decisions (proxy-executed with owner sign-off standing)
| # | When | Decision | Evidence | Status |
|---|---|---|---|---|
| D1 | 2026-09-08 | Sofrito Session = 90 min @ $400 (was 75 min); written action-plan deliverable; $267/hr effective, mid-band for established-consultant tier | pricing benchmark research; site localization | LIVE (copy deployed, verified) |
| D2 | 2026-09-08 | Payment processor = Stripe for sessions/services/projects (vs Gumroad 10%+fee): B2B invoice expectation, services not goods, ~$11.90 vs ~$52 all-in at $400 | cost comparison; tax-comfort analysis | LIVE (Gumroad DORMANT, legacy only) |
| D3 | 2026-09-08 | Sessions route via contact-form pipeline until Calendly URL provided; KV `site/config.session_url` swap-in ready | current booking dependency | LIVE (no URL yet) |
| D4 | 2026-09-08→09 | Booking automation (S19): `/api/calendly-webhook` w/ signature verify (t.v1 HMAC, 5-min tolerance), invitee.created/canceled → bookings + lead mirror + owner emails + webhooks; billing gated on KV `config/booking_billing` (default `'none'`) | staged; migration applied to remote D1 | **STAGED, NOT DEPLOYED** |
| D5 | 2026-09-08 | Data hygiene: probe/test lead removed from remote D1 | verified absent | DONE |
| D6 | 2026-09-09 | Lead-pool re-audit: all 15 D1 rows verified as QA/deploy artifacts (09-05 pipeline tests; 09-08 Zapier/publish/deploy tests + founder self-test). Prior "11 real leads" premise **RETRACTED** — no contact targets exist in D1. OD1 reframed to a demand-source decision. | full 15-row D1 `leads` pull + name/source/timestamp cluster analysis | DONE (all docs patched) |
| D7 | 2026-09-09 | Repo-scoped operating system installed per founder choice: `opencode.json` (default_agent=operator; edit/bash/task ask; external_directory deny; todowrite allow) + `.opencode/agents/` (operator/researcher/strategist/growth/builder/reviewer/deployer) + BUSINESS REASONING PROTOCOL into repo-root `AGENTS.md` (now the standing instruction set) | founder selection "Repo-scoped (SofritoStudio)"; customize-opencode skill schema | DONE — activates after **opencode restart** |
| D8 | 2026-09-09 | Installed dedicated **Social Content Operator** subagent (`.opencode/agents/social.md` per founder-supplied system): drafting/planning only, zero publish/account/DM/ad access, every public item Draft + Awaiting Founder Approval, publishing gated post-by-post via a separate (future) agent; supports ONLY existing food-business studio services | founder-supplied system prompt + launch command | DONE — activates after **opencode restart** |
| D9 | 2026-09-09 | **GA4 consent-gated snippet (gap A5)** in pivot-site: add `loadGA4()` to `public/js/site.js` using legacy GA4 ID `G-7XFBM9JMEV`, gated on `consentGiven()` / `ss_consent==='1'` so no gtag loads before opt-in (matches privacy.html claim); fires automatic page_view on consent and on boot for returning consenting visitors; `anonymize_ip` on | legacy `consent.js` port (verified GA_ID `G-7XFBM9JMEV`, consented pattern); pivot `consentGiven()` = `'1'` (verified); founder-selected gap; `node --check` passes | **DEPLOYED — now LIVE under D17** (`a7cb86fb-…`, 2026-09-10); consent-gated GA4 + auto page_view verified in live site.js |
| D10 | 2026-09-09 | **Stripe pipeline verification (todo 2):** prod `sofrito-studio` holds only 5 secrets (ADMIN_KEY, BUTTONDOWN_API_KEY, RESEND_API_KEY, TIKTOK_EVENTS_TOKEN, WEBHOOK_URL). **`STRIPE_WEBHOOK_SECRET` and `STRIPE_API_KEY` are NOT deployed** → invoice-trigger records milestones but never sends a Stripe invoice (worker L671 warning path), and any real Stripe webhook POST returns `missing signature` 400 (L738-741), so no Stripe revenue can land in D1. Webhook endpoint (`/api/stripe-webhook` + `invoice.paid`/`checkout.session.completed`/`charge.refunded`) must be configured in the Stripe dashboard. `CALENDLY_WEBHOOK_SIGNING_KEY` also absent (matches OD3 blocker) | Cloudflare Workers API secrets read (primary, names only, no values); worker `src/worker.js` L544-585/L648-671/L737-821; `wrangler secret list` returned `[]` twice — CLI anomaly, API authoritative | **VERIFIED FINDING** — Stripe pipeline INERT until founder sets both secrets + dashboard webhook; setting secrets = owner approval |
| D11 | 2026-09-09 | **`page_view` emission (gap F2)** in pivot-site: add `track('page_view', { referrer: document.referrer || null })` to `public/js/site.js` — called in the onReady boot (L408-410, fires for returning consenting visitors) and in the consent-ok accept branch (L175-182, first-load visitors right after opt-in). Single-fire guaranteed: `track()` no-ops unless `consentGiven() === '1'`, and `bindConsent` early-returns when consent already stored, so no page can emit twice | analytics contract `starter/docs/analytics.md` defines `page_view` (never emitted); site.js `track()` L40-54 is consent-gated (verified); both call sites verified; `node --check` passes | **DEPLOYED — now LIVE under D17** (`a7cb86fb-…`, 2026-09-10); `page_view` wiring verified in live site.js |
| D12 | 2026-09-09 | **guide_url 301 (gap F3)** VERIFIED — no code change needed: newsletter handler returns `guide_url = SITE_URL + '/freebies/digital-guide.md'` (worker L410); `public/_redirects` L42 `/free* → /contact.html 301` catches it (bytes verified this session); smoke-test `scripts/smoke-test-apis.js` L28 asserts `guide_url` contains `digital-guide`; live curl earlier confirmed digital-guide.md → 301 /contact.html. Chain = intended lead-magnet capture. Nuance (no action): `/guide` exact → `/blog/brand-story.html` (L13) while wildcard `/guide*` → `/contact.html` (L44) | worker L410; `_redirects` L42/L13/L44; smoke-test L28; live curl | `VERIFIED` — requirement already satisfied |
| D13 | 2026-09-09 | **env-drift README cross-check (gap F4)** FIXED: pivot-site README secrets block corrected to match `.env.example` — removed vestigial `GUMROAD_WEBHOOK_SECRET`, replaced `MAKE_WEBHOOK_URL` with `WEBHOOK_URL`, added `TIKTOK_EVENTS_TOKEN`, `STRIPE_API_KEY`, `STRIPE_WEBHOOK_SECRET`, `CALENDLY_WEBHOOK_SIGNING_KEY`, and a pointer that `.env.example` is the authoritative list (local-only vars like `OPENROUTER_API_KEY` stay in `.env`, not worker secrets). Stack table payments row now "Stripe (services + projects; Gumroad dormant)" per D2; "Any purchase (Gumroad/Stripe webhook)" → Stripe events only | D10 deployed-secrets list (API); `.env.example` L29-45; README L18/L38-46/L78 | `VERIFIED` — doc corrected (internal edit, no deploy) |
| D14 | 2026-09-09 | **day-14 check-in (gap F6)** = header-comment drift, NOT a missing deployed email: worker header (L26-28) claimed a "day-14 check-in" on the schedule, but `runLeadAutomations` sends only drip day 2/5/9 (DRIP_PLAN L1085-1089), the 24h owner reminder (day≥1, L1127), and day-1/day-30 post-delivery follow-ups for won leads (L1140-1162); no `follow-up-day14.html` exists in `src/emails/`. Header comment corrected to match shipped behavior. Removing a day-14 nurture email from the plan is logged as OD8 (owner call when real leads exist) | worker L26-28/L1085-1089/L1102-1165; `src/emails/` glob (no day14 file); observed drift vs D6 (zero real leads) | `VERIFIED` — header corrected (internal doc edit, no deploy) |
| D15 | 2026-09-09 | **Money truncation (gap F7) FIXED in code (not deployed):** revenue views used SQLite integer division (`SUM(amount_cents)/100` where `amount_cents` is INTEGER) so cents were truncated to whole dollars (e.g. 25045 → 250, dropping 45¢). Corrected to `/100.0` in `schema.sql` L135/L137/L147 (`v_pipeline`, `v_monthly_revenue`), recreated the views in new migration `0005_fix_money_views.sql` (drop + rebuild, immutable `0001` left pristine and superseded), fixed the admin inline query (`src/worker.js` L515), and replaced the digest's hardcoded `.00` with `(Number(x)||0).toFixed(2)` (L1179-1180). Remaining `/100` money sites: none (only `0001_initial.sql`, superseded). `node --check` passes. Impact is owner-facing only (v_pipeline → weekly digest; v_monthly_revenue → dashboard/admin) and latent until real revenue exists | schema.sql L135/137/147; `migrations/0005_fix_money_views.sql`; worker L515/L1179-1180; grep site-wide `/100`; `node --check` SYNTAX_OK | **DONE — DEPLOYED under D17** (migration 0005 applied + ledgered id=5; worker + dollar-view output live `a7cb86fb-…`, 2026-09-10) |
| D16 | 2026-09-09 | **Remotion deliverable = branded in-repo skill pack** (`pivot-site/skills/`, README + 6 `remotion-*` skills): founder's final instruction "go with the recommended option" supersedes the earlier "just a mapping doc" answer. Pack adapts the global `remotion-*` skills to AGENTS.md brand rules (tokens, formats 9:16/4:5/1:1, Path A deterministic text, Realistic Social Visual Standard, `DRAFT — AWAITING FOUNDER APPROVAL` gate). Legacy `remotion-abuelas-ipad` Starter Kit + "Abuela's iPad" ad content excluded by directive; non-vendored global skills (docs/interactivity/maps/markup/multimedia/saas) load globally when needed. No project/ad content in the pack — instructions only | founder choice (final message); `pivot-site/skills/` glob (7 files present); global skills inventory (12 `remotion-*`) | DONE — pack created (internal files, no deploy). Registration: junction into `pivot-site/.agents/skills/` per README (not yet linked) |

| D17 | 2026-09-10 | **GA4 + page_view + F7 deploy (founder-approved batch):** deployed worker F7 + assets from HEAD staging tree (D9 consent-gated GA4 snippet, D11 page_view emission, F7 money views). Migration `0005_fix_money_views.sql` applied to remote D1 + ledgered (id=5). Live version `a7cb86fb-97f9-4062-bd8f-3b0e1c3d6b00`; homepage 200, /api/health ok; revenue views now return dollars (v_pipeline revenue_mtd/30d columns + v_monthly_revenue verified via D1 MCP). Deployed-worker code re-verified: F7 hunks present (`SUM(amount_cents)/100.0`, digest `revenue_mtd_dollars`/`revenue_30d_dollars`), no D4 Calendly code. Assets carry GA4 + `page_view` + sessionBtn fallback line (live site.js bytes). D4 Calendly webhook and the 75→90-min HTML copy edits + sessionBtn hidden-line removal explicitly EXCLUDED (out of batch). revenue table currently empty (dollar views return 0) — latent until real revenue | wrangler deploy `a7cb86fb`; D1 migration 0005 (rows 5 ledger); live /api checks; deployed-worker code check; live site.js bytes | DONE — DEPLOYED (version `a7cb86fb-97f9-4062-bd8f-3b0e1c3d6b00`, 2026-09-10) |
| D18 | 2026-09-10 | **Sitemap consistency (gap S1) FIXED + COMMITTED (`4b3c3cd`, no deploy):** curated `public/sitemap.xml` was missing `work/spec-concept-03.html` even though it is live (200). Added the entry in the existing style (yearly/0.5, matching spec-concept-01/02) + aligned `scripts/gen_sitemap.py` to the verified canonical: now targets `pivot-site/public` (was nonexistent `deploy/`), keeps the `.html` suffix (live `/services.html`=200, `/work`=404), dry-run by default, `--write` required to apply, and no longer writes a stale root `sitemap.xml`. Verified all 24 committed URLs return 200 live; after fix the generated set (25) == committed set (25) - sets match. Pre-commit suite: 10/10 checks passed. Live sitemap feed still serves the OLD 24-URL list until the next approved deploy - sitemap contract for Search Console pending | live HEAD-request loop (24/24 = 200); `gen_sitemap.py` dry-run (25 URLs); compare script `sitemap-compare.py` (SETS MATCH); git `4b3c3cd` | DONE - COMMITTED (not yet live; rides next deploy) |
| D19 | 2026-09-10 | **Retire legacy `remotion-abuelas-ipad` "Abuela's iPad" 3D reel (D16 follow-up):** annotated `AbuelasIpad.tsx` top-of-file as RETIRED — legacy record only, do not render/develop/market. Reasons: (1) pitches the retired $9 Starter Kit + `STARTER15` coupon → violates pivot confinement; (2) Pixar-style 3D/cartoon/CGI → banned by the Realistic Social Visual Standard; (3) diaspora-home-cook / bilingual-cookbook framing → off-pivot. Not deleted (`build/` untracked; kept as record). Reel work continues as a NEW food-business-branding composition in `video-ads/`. | content-strategy pivot; D16 exclusion directive; Realistic Social Visual Standard; repo AGENTS.md | DONE — annotated in code, logged, no render/deploy |
| D20 | 2026-09-11 | **Canonical ads location = `C:\Users\josho\Downloads\Video\`** (founder directive). Approved CTA originals + excluded clips live in `Downloads\Video\CTA-drafts-DRAFT\`; publish-ready derived exports live ONLY under `growth/social/approved/CTA-2026-09-11-B01/publish-ready/` (internal copies — downloads/renders never modified, no duplicate bytes in repo). | founder directive on ad-material storage; verified folder structure (7 approved originals + 3 excluded v12771 clips); derived size set verified 7/7 | LIVE/RECORDED — originals untouched |

## Decision memo (D19) — Retire "Abuela's iPad" 3D reel
- **Decision:** retire `remotion-abuelas-ipad` as draft-legacy (file annotated, not deleted).
- **Why (evidence):** pitches the retired $9 Starter Kit + `STARTER15` (old-model resurrection forbidden), Pixar/3D/CGI visuals violate the Realistic Social Visual Standard, diaspora-cookbook framing is off-pivot; D16 already excluded this content from the Remotion skill pack.
- **Alternatives considered vs result:** leave as-is (violates standing rules) or delete files (destroys record) → chose annotate-in-place as legacy record + log decision.
- **Owner sign-off standing:** internal code edit; recorded as executed D19.
- **Next:** new food-business-branding reel in `video-ads/` — pending founder's option pick (separate memo).

## Decision memo (D20) — Canonical ads location
- **Decision:** the canonical home for Sofrito advertising/video material is `C:\Users\josho\Downloads\Video\` (founder directive). `CTA-drafts-DRAFT\` holds the 7 approved CTA originals + 3 excluded `v12771` clips; publish-ready derived exports live only under `growth/social/approved/CTA-2026-09-11-B01/publish-ready/` and are tracked in `asset-register.md`.
- **Why (evidence):** founder specified this location for ad material (`VERIFIED`); folder structure confirmed this session; derived size set verified 7/7 via ffprobe.
- **Alternatives considered vs result:** store renders only in Downloads (loses approval-package tracking) or duplicate into the repo (adds byte duplication) → chosen: originals stay in the founder's Downloads root, exports tracked in the approved package + asset register. Internal record of a founder directive, no deploy.
- **Owner sign-off standing:** recorded as executed decision D20.
- **Next:** per-platform captions for the 7 approved CTA ads (IG/FB/Pinterest/TikTok), then per-post publish go (founder approval still required).

## Open decisions (owner — each blocks a step)
| # | Decision | Options | Recommendation | Status |
|---|---|---|---|---|
| OD1 | Real-demand source (was "triage the 11 leads"; pool = QA, no targets) | (a) pursue social-scan enrichment prospects (DavIsa/El Inquieto, Growee, El Chilar HF, Paldy) — the only real people identified; (b) capture-validation move only (fix leaks, measure real traffic); (c) hold | (a) after the 3 leak fixes — evidence-led targeting | awaiting owner |
| OD2 | Retainer waitlist capture | (a) POST /api/leads capture, (b) keep inert | (a) — pure win, reversible | awaiting owner |
| OD3 | Session checkout | (a) deploy staged Calendly+Stripe bundle, (b) stopgap copy now + defer | (a) if inputs arrive in 30d, else (b) immediately | awaiting owner inputs (4) + go |
| OD4 | Brand Foundation (G3) | price / fold into Sofrito / drop | price in $1k–$1.5k band **after** pricing research | awaiting owner + research |
| OD5 | La Mesa policy | referral-only scarcity vs open volume | scarcity + referral incentive | awaiting owner |
| OD6 | Tier 3 digital products | approve / defer / kill | defer until first client wins; then revisit | awaiting owner |
| OD7 | Billing mode for bookings | charge-at-booking vs invoice-after | invoice-after for trust at first; revisit at scale | awaiting owner |
| OD8 | Day-14 nurture email (documented in worker header, never implemented) | (a) implement (new template + DRIP_PLAN step; founder-approved copy + deploy), (b) skip — drip 2/5/9 + day-1/30 suffices | (b) for now — zero real leads (D6); revisit when pipeline has real targets | awaiting owner |

## Required inputs awaiting owner (blocker detail for OD3)
1. Calendly 90-min event URL
2. Calendly webhook signing key
3. Stripe account + `STRIPE_API_KEY` secret
4. Billing mode (OD7)

## Standing rules (not decisions — constraints)
- Research gate: every decision 100% research-based; content best-practices gate before anything commits.
- Pivot confinement: no resurrection of the old $47-product/retail model.
- Sign-off before: deploys, commits (of consequence), pricing, launches, external billing, campaign spend, external contact.
- Data hygiene: record invoices/payments in D1 as they occur; never fabricate numbers.


| D21 | 2026-09-12 | Deploy SOP written (ops/sops/deploy.md): canonical 
pm run deploy from pivot-site, two-config gotcha (parent wrangler.jsonc vs. pivot-site/wrangler.toml), rollback note (comment routes + redeploy ../cloudflare), CI-on-main reference, un_worker_first/html_handling paired-change rule. Home AGENTS.md reorg: moved four social sections (Realistic Social Visual Standard, Prompt Protocol, Spelling Protocol, Carousel Typography) VERBATIM to growth/social/SOCIAL-VISUAL-STANDARD.md (1501 lines); kept design tokens + scroll-viewport; added deploy-SOP pointer; approval-gate converted to approved-runbook (VERIFIED/INFERENCE/ASSUMPTION + decision-log labels) | pivot-site/package.json (deploy script verified); wrangler.toml (assets public/, custom_domain, D1 config, rollback header comment); bash Get-ChildItem -Force (.opencode/agents/ files confirmed); new SOCIAL-VISUAL-STANDARD.md byte-match verified (1501 lines) | DONE (internal docs, no deploy). Approval-gate model: executing a documented SOP = pre-approved; any deviation requires approval. Social content stays Draft/Awaiting Approval until explicit sign-off (epo AGENTS.md, .opencode/agents/social.md).



| D22 | 2026-09-12 | Pinterest verification switched from meta tag to TXT record (pinterest-site-verification=2b1a51f24ce4babf33e28a270bb9be2a at @ root). Meta tag remains live (public/*.html line 24, verified 7cb86fb). No MCP DNS tool available; record must be added via Cloudflare dashboard/API. Approval: explicit user directive (switch to TXT) + verified value match. | meta tag live (https://sofritostudio.com/ line 24); exact content 2b1a51f24ce4babf33e28a270bb9be2a; user directive (VERIFIED) | DONE — record added externally (pending propagation + Pinterest retry). DNS change = live-system action; user sign-off confirmed.
| D23 | 2026-09-12 | Sprint page (public/sprint.html) LIVE with founder-verbatim copy + all CTAs on the new Calendly booking link, served at clean URL /sprint. (a) Copy fixes applied: H2 -> "The Deliverables: What You Get for $997"; hosting bullet -> "Zero monthly hosting fees (We build it on infrastructure that scales for free)."; process H2 -> "The Process: 3 Steps to Launch"; Step 1 -> "The Taste Test (Paid Discovery)" + "$300 Sofrito Session ... 60-minute call ... credits directly toward your final project."; Step 2 -> "You go back to the kitchen; we go to the studio ... in two days."; Step 3 -> "one round of precise revisions ... ready to take orders."; pricing body -> "$300 Strategy Session ... remaining $697 ... 48 hours"; pricing CTA -> "Start Your Brand Sprint"; FAQ Q2 -> "simple Google Sheet"; FAQ Q3 trailing sentence removed. (b) All 4 CTAs (nav, hero, pricing, sticky mobile bar) point to https://calendly.com/j-ortiz1148/sofrito-strategy-session (was /contact.html + [CALENDLY_URL] placeholder). (c) Worker route added: /sprint serves sprint.html directly (extensionless, mirrors / -> /index.html) - clean URL now 200, was 404. (d) Calendly event "Sofrito Strategy Session" created via API (60 min, active, color #d93b26, custom question: "Please share anything that will help prepare for our meeting."). Palette kept on compiled Tailwind tokens (site.css is a minified Tailwind build - arbitrary hex classes don't render). | founder verbatim requirements; deployed versions 7723cca1 + 677db0d5; live curl /sprint -> 200, 4 calendly hrefs, 0 placeholders, 0 /contact.html; Calendly API event_types create (URI 6ebf2993-92cc-4d51-bafa-ac055bb6019f); D21 pre-approved runbook (deploy SOP) | DONE - LIVE (sofritostudio.com/sprint, version 677db0d5-2647-46d0-aa6c-4a4ed8c429da). FOLLOW-UP (owner, manual): Calendly event is_paid: false - founder must enable payment collection (Stripe/Square) in Calendly UI to gate the $300; Zapier intake + $697 invoice runbook in ops/sops/sprint-intake.md (see D24). |
| D24 | 2026-09-12 | Sprint intake SOP WRITTEN (ops/sops/sprint-intake.md) - completes D23 follow-up: pipeline (booking $300 gate -> Zapier welcome email + Google Form + Drive folder "«Client» — Brand & Web Sprint" + Sheets tracker row -> 60-min session -> $697 Stripe invoice before 48h sprint -> optional $50/mo Site Care upsell), ready-to-paste 4-question intake Google Form, $697 invoice template text, and a blockers table. Founder-manual steps documented as NOT live at time of writing: (1) Calendly $300 payment gate (is_paid: false - API cannot set; Stripe/Square via Calendly UI), (2) Zapier account connections + Zap creation, (3) Stripe secrets/webhook for automated invoice (still blocked per D10), (4) invoicing mechanism choice. | D23 follow-up reference; transcript blueprint "Relevant Setup Requirements" (gateway, intake, invoice); deploy.md style precedent | DONE - INTERNAL DOC (no deploy; no live-system change). Next (owner, manual): enable the $300 gate + connect Zapier per the SOP checklist.
| D25 | 2026-09-12 | Client deliverables standardized on a STATIC HTML/Tailwind template matching the deployed Workers stack (Option A) - rejects the transcript's Next.js "Food Startup Boilerplate". Rationale per founder decision memo: no Node runtime/SSR/maintenance; a static template matches the existing Cloudflare pipeline, forks per client in minutes, keeps the "zero monthly hosting fees" claim true, and speeds the 48h sprint (hours not days). Risk mitigation: no server errors/build failures/egress compute surprises. | founder-approved decision memo (owner approval given 2026-09-12); deployed stack verified Workers + static HTML/Tailwind (AGENTS.md token/stack constraints); transcript blueprint called for Next.js (conflict noted) | APPROVED (Owner, 2026-09-12). DONE 2026-09-12: built client-templates/client-site-basic.html (Variant A, bright - self-contained token-based single-file template, 54 fork tokens, accordion menu, sticky mobile CTA, no build step) and client-templates/client-site-artisan.html (Variant B, dark artisan - Deep Red #910A0A / charcoal + Honey Gold #E0A020, 17 fork tokens, zero CDN refs). Both verified balanced tags + token sweep; sprint-intake.md amended with a "Client site build" section referencing both variants for the 1-Page Engine deliverable, picked by client brand personality. OPEN: Pages-vs-Workers per-deployment choice at 1st client deploy (deploy.md two-config gotcha applies - pinned --config wrangler.toml).
| D26 | 2026-09-12 | **TWO-OFFER CONVERSION DEPLOYED + LIVE (founder-approved "Yes, deploy now"):** site now sells exactly two offers - Sofrito Session ($400, 90 min, written action-plan) and Brand & Web Sprint ($997). Session $400 up front books the spot and is credited toward a Sprint booked within 30 days; Sprint balance $597 settles before launch; files release after the balance clears; total $997. Invoices use session/final internal milestone keys (SESSION_CENTS=40000; split = {session: min(price,40000), final: price-session} -> 40000/59700). Deployed Version `5e4ea067-ac8c-41bd-ab3a-35d65fd90b23` (15 files). | founder approval "Yes, deploy now" (2026-09-12) + "Defer" internal-docs choice; live curl /services (both offers) + /faq (payment/milestone answers); worker node --check; versions 5e4ea067 (deploy); sitemap live fetch + gen_sitemap.py dry-run; sprint-intake.md figures aligned (D24 doc) | DONE - DEPLOYED LIVE (version `5e4ea067-ac8c-41bd-ab3a-35d65fd90b23`, 2026-09-12). FOLLOW-UP 1 (owner, manual): Calendly gate to $400 - event still configured at $300 / is_paid:false; Zapier intake + $597 invoice cleanup (sprint-intake.md). FOLLOW-UP 2 RESOLVED (owner-approved deploy 2026-09-12): sprint.html added to live sitemap - local 26 URLs deployed as Version `ee2325fa-4bf8-4d5e-b7c0-da812440523d`; live sitemap.xml verified 26 URLs incl. sprint.html. |


## Decision Memo: Redeploy compact consent bar (line 168 revert) to live
**Status:** Executed
**Decision:** Redeployed the founder-approved compact consent-bar fix (public/js/site.js:168 restored to `sm:flex-row sm:items-center`) to https://sofritostudio.com.
**Reason (evidence):**
- VERIFIED (grep, this session) — on-disk line 168 = `fixed bottom-0 left-0 right-0 z-60 bg-white ... sm:flex-row sm:items-center ...'' ; founder-approved baseline restored.
- VERIFIED (node --check, this session) — `public/js/site.js` SYNTAX OK (disk matches baseline).
- VERIFIED (live HTTP 200 + served markup) — https://sofritostudio.com now serves the compact bar; `Cookie Preferences` span present in live JS bundle.
**Alternatives considered:** | Option | Why not chosen | | Redeploy + drop inert `transition` on OK button | Founder did not request the drop; skipped by default. |
**Cost/Impact:** No cost; restored founder-approved desktop compact layout. Live stack CPU/cache inside wrangler-managed deploy.
**Risks (labeled):**
- (low) Live deploy — file was node-checked and matches the exact approved baseline.
**Owner approval:** Given (2026 — founder: `redeploy`).
**Result / follow-ups:**
- Deployed via `npm run deploy` (wrangler 4.129.0), new version `c03c43c6`, upload 1 file, 0 errors.
- Next: monitor consent interactions in ops/scorecard; archive this memo's diff when next weekly review occurs.
